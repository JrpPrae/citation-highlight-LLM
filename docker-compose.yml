services:
  citation-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: citation-container
    ports:
      - "8000:8000"
    env_file:
      - ./app/.env
    restart: unless-stopped
